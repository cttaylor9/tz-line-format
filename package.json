{
  "name": "tz-line-format",
  "version": "0.1.0",
  "description": "Normalizes messy timezone-aware timestamps into canonical ISO 8601 strings, one per line.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "node --experimental-strip-types --test test/normalize.test.ts"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "typescript": "^5.7.2"
  },
  "license": "MIT",
  "author": "cttaylor9"
}
